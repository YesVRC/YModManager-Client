export type Pack = {
    icon: string;
    name: string;
}

export type ForgeManifest = string[][]

export type GithubRelease = {
    name: string;
    browser_download_url: string;
}

export const JavaPath = "%s/java/%s/bin/java.exe"
